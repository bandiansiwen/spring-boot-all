1、DataSourceConfig配置

2、创建 DBTypeEnum 枚举定义主从库

3、创建 DataSourceContextHolder 来保存 DBTypeEnum

4、继承 AbstractRoutingDataSource

5、通过注解 DSRoute 来标识走master/slave

6、创建 DataSourceRouteAspect，来处理注解 DSRoute

### 指定数据源方式：
1. 通过 DSRoute 注解
2. 通过 DataSourceContextHolder.customSet 设置数据源（优先级别最高）